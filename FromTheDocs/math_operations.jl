# https://docs.julialang.org/en/v1/manual/mathematical-operations/
x = 10.78
# round up, returning same type
println(round(x))
# round up, returning specified type
println(round(Int, x))
# round down, return same type
println(floor(x))
# round up, return same type
println(ceil(x))
